# 🎮 เกมส์เติมคำภาษาไทย - Thai Word Game

เกมส์เติมคำภาษาไทยแบบสนุก ออกแบบมาเพื่อพัฒนาทักษะภาษาไทยของผู้เล่น มีคลังคำศัพท์กว่า 100+ คำแบ่งเป็น 10 ด่าน

---

## 📸 ฟีเจอร์หลัก

✅ **ระบบสมัครสมาชิกและเข้าสู่ระบบ** - บันทึกความก้าวหน้าในระบบฐานข้อมูล  
✅ **10 ด่านโปรแกรม** - เริ่มต้นจากคำง่ายไปยังคำยาก  
✅ **ระบบปลดล็อคด่าน** - ปลดล็อคด่านใหม่หลังตอบคำได้ 5 ข้อ  
✅ **เสียงเพลงพื้นหลัง** - เพลงเบาเบาตลอดการเล่น (สามารถปิดได้)  
✅ **เสียงปุ่มและคำ** - เสียง beep เมื่อคลิกปุ่มและเสียงออกเสียงคำ  
✅ **ระบบคะแนน** - บันทึกคะแนนและสถิติการตอบถูก  
✅ **เลือกตัวละคร** - 6 ตัวละครให้เลือก (รูปภาพ)  
✅ **รองรับมือถือ** - ดีไซน์ Responsive ทำงานได้บน iPhone, Android  

---

## 🛠️ วิธีแก้ไขและปรับปรุง

### 1. **ปรับขนาดการ์ดด่านให้เหมือนกับการ์ดแสดงข้อมูล (Mobile)**
   - **สถานที่:** `css/styles.css` (บรรทัด 1081-1120)
   - **สิ่งที่แก้:**
     - เพิ่มขนาดการ์ดให้กว้าง 90vw เต็มหน้าจอ
     - เพิ่มช่องว่างระหว่างปุ่ม (gap) ให้สม่ำเสมอ
     - ปรับ padding และ border-radius ให้สม่ำเสมอ
   - **ผลลัพธ์:** ด่านและสถิติดูสวยงามเท่าๆ กันบนมือถือ

### 2. **ปรับดีไซน์ปุ่มและเนื้อหาในการ์ดด่าน**
   - **สถานที่:** `css/styles.css` (บรรทัด 1081-1155)
   - **สิ่งที่แก้:**
     - เพิ่มสีไล่เฉด (gradient) สำหรับปุ่ม
     - เพิ่มเงา (box-shadow) ให้ลึกขึ้น
     - เพิ่มเอฟเฟกต์ hover และ active บนปุ่ม
     - ออกแบบปุ่มเสียงให้เป็นวงกลมสีขาวภายในปุ่ม
   - **ผลลัพธ์:** ปุ่มดูสามมิติและสวยงาม

### 3. **เล่นเสียงเพลงทุกหน้า**
   - **สถานที่:** `js/game.js` (บรรทัด 29-45)
   - **สิ่งที่แก้:**
     - เพิ่ม `startBackgroundMusic()` ในฟังก์ชัน `window.onload`
     - เล่นเพลง `เสียงคำตอบ/เพลง.mp3` วนลูปตลอด
   - **ผลลัพธ์:** เพลงเล่นตั้งแต่โหลดหน้าและเล่นต่อในทุกหน้า

### 4. **ลดระดับเสียงเพลงพื้นหลัง**
   - **สถานที่:** `js/game.js` (บรรทัด 579-590)
   - **สิ่งที่แก้:**
     - ลดระดับจาก `0.15` (15%) เป็น `0.08` (8%)
   - **ผลลัพธ์:** เสียงเพลงไม่กลบตัวอักษรและเสียงปุ่ม

### 5. **เพิ่มเสียงปุ่มและเสียงคำ**
   - **สถานที่:** `js/game.js`
     - `playAudio()` (บรรทัด 1385-1398) - เพิ่ม `volume = 0.8`
     - `playButtonClickSound()` (บรรทัด 1422-1437) - เสียง beep สำหรับปุ่ม
     - `playCorrectSound()` (บรรทัด 1315-1333) - เพิ่มระดับเสียงจาก 0.3 เป็น 0.5
     - `playWrongSound()` (บรรทัด 1350-1368) - ลดระดับเสียงจาก 0.3 เป็น 0.2
   - **ผลลัพธ์:** เสียงคำชัดขึ้น, ปุ่มมีเสียง beep ตอบสนอง, เสียงพลุดังชัด

### 6. **ปรับ toggleSound ให้ปิดแค่เสียงเพลง**
   - **สถานที่:** `js/game.js` (บรรทัด 564-568, 1409-1422)
   - **สิ่งที่แก้:**
     - แยก `soundEnabled` (คำ/ปุ่ม) และ `backgroundMusicEnabled` (เพลง)
     - ปุ่มเสียงปิดเฉพาะเพลง ไม่ปิดเสียงคำและปุ่ม
   - **ผลลัพธ์:** ผู้เล่นสามารถปิดเพลงแต่ยังได้ยินเสียงคำและปุ่ม

---

## 🚀 วิธีใช้งาน

### ติดตั้งและรัน (Development Mode)

#### 1. **Backend Setup**

```bash
# เข้าไปในโฟลเดอร์ backend
cd backend

# ติดตั้ง dependencies
npm install

# สร้างไฟล์ .env (ก็อปจาก .env.example)
# แล้วเติม MONGODB_URI และ JWT_SECRET ของคุณ

# เรียกใช้ backend
npm start
```

**Backend จะรันที่:** `http://localhost:5000`

#### 2. **Frontend Setup**

```bash
# ไปที่โฟลเดอร์ root
cd ..

# ติดตั้ง Live Server หรือรัน server อื่น
# วิธี 1: ใช้ VS Code Live Server Extension
#   - คลิกขวา index.html → Open with Live Server

# วิธี 2: ใช้ Node.js http-server
npm install -g http-server
http-server .
```

**Frontend จะรันที่:** `http://localhost:8080` (หรือตามที่ Live Server บอก)

#### 3. **ตั้งค่า Backend URL**

แก้ไข `js/api.js` เปลี่ยน Base URL:

```javascript
// บรรทัดแรกของ api.js
const API_BASE_URL = 'http://localhost:5000/api'; // ในตัวอักษร development
// หรือ 'https://your-backend-url.onrender.com/api'; // ในตัวอักษร production
```

### ใช้งาน

1. **เปิดเบราว์เซอร์** และไปที่ Frontend URL
2. **สมัครสมาชิก** หรือ **เข้าสู่ระบบ**
3. **เลือกตัวละครและตั้งชื่อ**
4. **เลือกด่าน** เพื่อเริ่มเล่น
5. **กรอกคำตอบ** และคลิกตรวจสอบ
6. **ปลดล็อคด่านใหม่** เมื่อตอบถูก 5 ข้อ

### ปุ่มและการตั้งค่า

- **🔊 ไอคอนเสียง:** ปิด/เปิดเสียงเพลงพื้นหลัง (เสียงคำและปุ่มยังเล่นต่อ)
- **⚙️ ไอคอนตั้งค่า:** แก้ไขโปรไฟล์ (ชื่อ, ตัวละคร)
- **🚪 ออกจากระบบ:** ออกจากบัญชี

---

## 🗂️ โครงสร้างไฟล์

```
Games Word/
├── index.html              # หน้าหลัก HTML
├── css/
│   └── styles.css          # สไตล์ทั้งหมด
├── js/
│   ├── api.js              # API calls และการตั้งค่า
│   └── game.js             # ลอจิกเกมหลัก
├── backend/
│   ├── server.js           # Express server
│   ├── package.json        # Dependencies
│   └── models/
│       ├── user.js         # Schema ผู้ใช้
│       ├── progress.js     # Schema ความก้าวหน้า
│       └── leaderboard.js  # Schema คะแนนสูง
├── เสียงคำตอบ/            # เสียง mp3 ของคำตอบ
├── เสียงพยัญชนะ/          # เสียง mp3 ของพยัญชนะ
├── วรรณยุกต์สระ/          # เสียง mp3 ของสระและวรรณยุกต์
├── ปุ่ม button/            # รูปปุ่มและ UI
├── ภาพประกอบ คำศัพท์/     # รูปประกอบคำศัพท์
└── README.md               # ไฟล์นี้
```

---

## 🔌 API Endpoints

### Authentication
- `POST /api/auth/register` - สมัครสมาชิก
- `POST /api/auth/login` - เข้าสู่ระบบ

### User
- `GET /api/user/profile` - ดึงโปรไฟล์ผู้ใช้
- `PUT /api/user/profile` - อัปเดตโปรไฟล์

### Progress
- `GET /api/progress` - ดึงความก้าวหน้า
- `POST /api/progress` - บันทึกความก้าวหน้า

### Leaderboard
- `GET /api/leaderboard` - ดึง Top 10 scores

---

## 🌐 Deploy (Production)

### Deploy Frontend ด้วย Netlify/Vercel

```bash
# Build files (ส่วน frontend เป็น static files)
# Upload ไปที่ Netlify/Vercel ทีละ drag & drop
```

### Deploy Backend ด้วย Render/Railway

ดูรายละเอียดใน `deployment.md`

```bash
# ตัวอย่าง Render:
# 1. สร้าง render.yaml
# 2. เชื่อม GitHub repository
# 3. ตั้งค่า environment variables (MONGODB_URI, JWT_SECRET)
# 4. Deploy
```

---

## 🐛 การแก้ไขปัญหา

### เสียงคำไม่เล่น
- ตรวจสอบว่าไฟล์เสียง mp3 อยู่ในโฟลเดอร์ที่ถูกต้อง
- ตรวจสอบเบราว์เซอร์อนุญาตให้เล่นเสียง

### เพลงวนลูปไม่ได้
- ตรวจสอบว่าไฟล์ `เสียงคำตอบ/เพลง.mp3` มีอยู่

### Backend ไม่เชื่อมต่อ
- ตรวจสอบ MONGODB_URI ถูกต้อง
- ตรวจสอบ JWT_SECRET ตั้งค่าแล้ว
- ตรวจสอบ Backend URL ใน `js/api.js` ถูกต้อง

### ด่านไม่ปลดล็อค
- ตรวจสอบตอบถูก 5 ข้อขึ้นไป
- รีเฟรชหน้า เพื่อให้บันทึกสถานะ

---

## 📝 หมายเหตุเพิ่มเติม

- ทั้งหมดใช้ **Font Kanit/Prompt** สำหรับภาษาไทย
- **Responsive Design** ทำงานได้ดีบน mobile, tablet, desktop
- **NoSQL (MongoDB)** เก็บข้อมูลผู้ใช้และคะแนน
- **JWT Authentication** เพื่อความปลอดภัย

---

## 📞 ติดต่อ / สนับสนุน

หากมีปัญหา หรือต้องการแจ้งข้อบกพร่อง กรุณาสร้าง Issue ใน GitHub

---

**Made with ❤️ for Thai Learning**
